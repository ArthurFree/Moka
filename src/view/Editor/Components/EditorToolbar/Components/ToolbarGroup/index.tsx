import React from 'react';
import './index.scss';

export default class ToolbarGroup extends React.Component {
    render(): React.ReactNode {
        return (
            <div className="toolbar-group-wrap"></div>
        );
    }
}
